#include "main.h"

/**
 * is_palindrome - is the string palindrome or not
 *
 * @s: string/ function parameter
 *
 * Return: 1 if string is palindrome
 * 		0 if string is not palindrome
*/

int is_palindrome(char *s);
